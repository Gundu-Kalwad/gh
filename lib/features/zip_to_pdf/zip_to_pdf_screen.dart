import 'package:flutter/material.dart';

class ZipToPdfScreen extends StatelessWidget {
  const ZipToPdfScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Zip to PDF'),
      ),
      body: const Center(
        child: Text('Zip to PDF feature coming soon!'),
      ),
    );
  }
}
